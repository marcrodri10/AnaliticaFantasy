import { BuildOptions } from 'vite';
export interface AppConfig {
    WS_CLIENT: string;
    DEV_MODE: boolean;
    BUILD_DIR: string;
    BUILD_URL_PREFIX: string;
    JS_ATTRS: {
        [key: string]: string;
    };
    CSS_ATTRS: {
        [key: string]: string;
    };
    STATIC_LOOKUP: boolean;
    INSTALLED_APPS: Record<string, string>;
    HOT_FILE: string;
}
export interface PluginConfig {
    /**
     * The path or paths of the entry points to compile.
     */
    input: string | string[];
    /**
     * The root path of the project relative to the `vite.config.js` file
     */
    root?: string;
    /**
     * If the aliases should be added in the `jsconfig.json` or not
     */
    addAliases?: boolean;
    /**
     * Path to python executable
     */
    pyPath?: string;
    /**
     * Path to python executable
     */
    pyArgs?: Array<string>;
    /**
     * Full reload options
     */
    reloader?: boolean | ((file: string) => boolean);
    watch?: string[];
    delay?: number;
}
export interface InternalConfig extends PluginConfig {
    /**
     * Configuartion provided in project's `settings.py`
     */
    appConfig: AppConfig;
}
export type DevServerUrl = `${'http' | 'https'}://${string}:${number}`;
export declare function resolvePluginConfig(config: PluginConfig, appConfig: AppConfig): Promise<InternalConfig>;
export declare function resolveBuildConfig(config: InternalConfig, front?: BuildOptions): BuildOptions;
